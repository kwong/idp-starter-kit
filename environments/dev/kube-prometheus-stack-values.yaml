# kube-prometheus-stack environment-specific Helm value overrides for dev.
# These patches are applied on top of components/kube-prometheus-stack/base/values.yaml.
# See https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/patches/ for syntax.
